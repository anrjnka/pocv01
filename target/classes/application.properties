server.port=8189

spring.security.enabled=false

# PostgreSQL Configuration
spring.datasource.url=jdbc:postgresql://103.102.15.18:5432/Ancolitas
spring.datasource.username=junuka
spring.datasource.password=Ancolitas
spring.datasource.driver-class-name=org.postgresql.Driver
spring.jpa.hibernate.ddl-auto=update
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true

# Configure HikariCP connection pool
spring.datasource.hikari.max-lifetime=1800000  
spring.datasource.hikari.connection-timeout=30000 
spring.datasource.hikari.idle-timeout=600000  
spring.datasource.hikari.validation-timeout=5000  
spring.datasource.hikari.validation-query="SELECT 1" 
spring.datasource.hikari.minimum-idle=5  
spring.datasource.hikari.maximum-pool-size=10 

# Disable DataSource auto-configuration
#spring.autoconfigure.exclude=org.springframework.boot.autoconfigure.jdbc.DataSourceAutoConfiguration

# Enable Basic Authentication and set a default user
spring.security.user.name=admin
spring.security.user.password=ancolitas
spring.security.user.roles=USER
